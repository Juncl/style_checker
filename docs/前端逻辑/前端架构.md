# 前端架构文档

> 本文档描述 `client/src/` 目录下前端代码的分层架构、模块职责、组件关系与数据流。

---

## 一、技术栈

| 类别 | 技术 |
|---|---|
| 框架 | Vue 3（Composition API，`<script setup>`） |
| 路由 | Vue Router 5 |
| UI 组件库 | Element Plus |
| HTTP | Axios（封装于 `api/request.ts`） |
| 构建工具 | Vite + vue-tsc |
| 语言 | JavaScript（Vue 文件）/ TypeScript（api、init、router） |

---

## 二、目录结构

```
client/src/
├── main.ts                          # 应用入口，创建 Vue 实例，注册插件
├── App.vue                          # 根组件，只含 <RouterView />
├── vite-env.d.ts                    # Vite 环境类型声明
│
├── router/
│   └── index.ts                     # 路由配置，/consistency 路由 + 通配重定向
│
├── api/                             # 接口层（与后端通信，全局复用）
│   ├── constants.ts                 # 平台类型 / 映射常量（PlatformKey / ZH_BY_KEY 等）
│   ├── request.ts                   # axios 实例 + 拦截器 + 通用 get/post 函数
│   └── index.ts                     # 业务接口（fetchCases / checkCase / checkUpload 等）
│
├── styles/                          # 全局样式
│   ├── app.css                      # 页面级通用样式
│   └── image-panel.css              # 图片对比面板专属样式
│
├── assets/                          # 静态图片资源
│
└── views/
    ├── utils/
    │   └── constants.ts             # 视图层共享常量（CASE_NAMES / DEBUG_COLORS / FILE_SLOTS）
    │
    └── consistency/                 # 设计还原检查功能模块（路由 /consistency）
        ├── ConsistencyView.vue      # 视图入口，状态管理中枢
        │                            # ⚠️ 第63-77行有 #pixso_render 占位 div（Pixso截图锚点），禁止删除或移动
        │
        ├── init/                    # 页面初始化逻辑
        │   ├── index.ts             # 串行编排 5 步初始化，导出 initApp()
        │   ├── checkAuth.ts         # 登录校验（检查 uiplusToken）
        │   ├── mountPixso.ts        # Pixso 渲染器挂载（预留）
        │   ├── detectIframe.ts      # iframe 环境检测
        │   ├── restorePlatform.ts   # 平台缓存读写（cachePlatform）
        │   └── trackEntry.ts        # 入口埋点（URL 三参数打点）
        │
        └── components/              # UI 组件
            ├── AppLayout.vue        # 顶部导航栏 + 插槽布局壳
            ├── UploadPage.vue       # 上传页（Step1-4 + 传送码 + 案例列表）
            ├── ReportPage.vue       # 报告页（图片对比 + 差异报告 + debugger）
            ├── DiffReport.vue       # 差异列表（筛选 / 搜索 / 低置信开关）
            ├── ImagePanel.vue       # 图片面板（Canvas 叠加层 + 节点检查器）
            ├── NodeTree.vue         # 节点树（搜索 + 仅匹配过滤）
            └── SummaryCard.vue      # 还原度评分卡片（圆形进度 + 统计数字）
```

---

## 三、分层架构图

```
┌────────────────────────────────────────────────────────────────┐
│                         浏览器 / 用户                           │
└────────────────────────────┬───────────────────────────────────┘
                             │ HTTP 请求 / 用户交互
┌────────────────────────────▼───────────────────────────────────┐
│  入口层  main.ts                                                │
│  创建 Vue App → use(ElementPlus) → use(router) → mount(#app)   │
└────────────────────────────┬───────────────────────────────────┘
                             │
┌────────────────────────────▼───────────────────────────────────┐
│  根组件  App.vue                                                │
│  <RouterView />  ← 纯路由出口，无业务逻辑                       │
└────────────────────────────┬───────────────────────────────────┘
                             │ 路由匹配
┌────────────────────────────▼───────────────────────────────────┐
│  路由层  router/index.ts                                        │
│  /consistency → ConsistencyView                                │
│  /*           → redirect /consistency                          │
└────────────────────────────┬───────────────────────────────────┘
                             │
┌────────────────────────────▼───────────────────────────────────┐
│  视图层  ConsistencyView.vue                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  状态：result / uploadFiles / currentPlatform / pairs …  │  │
│  │  逻辑：selectCase / runUpload / onPlatformSwitch / …     │  │
│  └─────────┬──────────────────────────────┬─────────────────┘  │
│            │ onMounted                    │ result 有无切换     │
│   ┌────────▼────────┐            ┌────────▼─────────┐         │
│   │   init/         │            │  UploadPage  或   │         │
│   │   initApp()     │            │  ReportPage       │         │
│   └─────────────────┘            └───────────────────┘         │
└────────────────────────────┬───────────────────────────────────┘
                             │ API 调用
┌────────────────────────────▼───────────────────────────────────┐
│  API 层  api/                                                   │
│  request.ts → axios 实例（自动注入 Authorization: Bearer token）│
│  index.ts   → fetchCases / checkCase / checkUpload / imageUrl  │
│  constants.ts → PlatformKey / PLATFORMS / ZH_BY_KEY …         │
└────────────────────────────┬───────────────────────────────────┘
                             │
┌────────────────────────────▼───────────────────────────────────┐
│  后端  /api/*  (Express, port 3012)                             │
└────────────────────────────────────────────────────────────────┘
```

---

## 四、组件树

```
App.vue
└── RouterView
    └── ConsistencyView.vue           ← 状态管理中枢
        └── AppLayout.vue             ← 顶部导航 + 插槽
            ├── UploadPage.vue        ← 仅在 result=null 时渲染
            │   └── （无子组件，内含拖拽/传送码/案例列表 UI）
            └── ReportPage.vue        ← 仅在 result 有值时渲染
                ├── SummaryCard.vue   ← 还原度评分 + 统计
                ├── ImagePanel.vue    ← 设计侧图片（Canvas叠加）
                ├── ImagePanel.vue    ← 开发侧图片（Canvas叠加）
                ├── NodeTree.vue      ← 节点树（双侧各一棵）
                └── DiffReport.vue   ← 差异列表
```

---

## 五、初始化流程

`ConsistencyView.vue` 的 `onMounted` 调用 `initApp()`，内部**严格串行**执行 5 步：

```
onMounted
    │
    ▼
1. checkAuth()         检查 localStorage.uiplusToken，无则调登录接口（预留）
    │
    ▼
2. mountPixso()        挂载 Pixso 渲染器（预留空函数）
    │
    ▼
3. detectIframe()      window.self !== window.top 判断，结果写入 window.__inIframe
    │
    ▼
4. restorePlatform()   读 localStorage.cachePlatform，返回上次平台 key
    │
    ▼
5. trackEntry()        URL 同时含 deliverableId / pageId / versionId 时调打点接口
    │
    ▼
返回 { platform }
    │
    ▼
currentPlatform.value = platform
fetchCases(platform)
```

---

## 六、数据流

### 6.1 上传文件分析

```
用户上传开发侧 JSON
    │ onStepPicked({ type: 'arkuiJson', file })  [ConsistencyView, async]
    ▼
detectPlatformFromJson(file)
    │ 解析 JSON 根节点：deviceType / name / content.width 判断平台
    ▼
若检测到平台与当前不同：
    ├── currentPlatform.value = detected
    ├── savePlatform(detected)   → localStorage.cachePlatform
    ├── cases.value = []         → 立即清空，防止旧 case 用新平台请求图片
    └── fetchCases(detected)     → 重新加载案例列表
    │
    ▼ （等待图片上传）
用户上传开发侧图片
    │ onStepPicked({ type: 'arkuiImage', file })
    ▼
json + image 均已就绪？
    ├── 是 → triggerDevPreview(files)   调 /api 解析节点，渲染预览
    └── 否 → 等待
```

> ⚠️ **关键约束**：无论平台是什么，**必须 JSON + 图片都上传完毕**才会触发节点预览接口。旧逻辑中 web 平台只需 JSON 即触发已废弃。

```
点击"开始对比"
    │ runUpload(platform)
    ▼
POST /api/check/upload (multipart)
    ▼
result.value = 返回数据
    │
    ▼
UploadPage 隐藏 → ReportPage 渲染
```

### 6.2 选择内置案例

```
用户点击案例
    │ emit('select-case', id)
    ▼
selectCase(id)
    │ POST /api/check/case/:id
    ▼
result.value = 返回数据 → ReportPage 渲染
```

### 6.3 平台切换

```
右侧下拉框 @change
    │ onPlatformSwitch(platform)  [UploadPage]
    ▼
emit('platform-switch', platform)
    │
    ▼
onPlatformSwitch(platform)        [ConsistencyView]
    ├── currentPlatform.value = platform
    ├── savePlatform(platform)    → localStorage.cachePlatform
    ├── cases.value = []          → 立即清空，防止旧 case 图片用新平台 URL 请求（404）
    └── fetchCases(platform)      → 重新加载案例列表
```

> ⚠️ **关键顺序**：`currentPlatform` 更新后必须先清空 `cases`，再等待 `fetchCases` 异步返回。否则过渡期间旧平台的 case 会用新平台参数请求缩略图，导致 404。

### 6.4 节点点击联动

```
ImagePanel / NodeTree 节点点击
    │ emit('arkui-node-click' / 'design-node-click', nodeId)
    ▼
ConsistencyView.onArkuiNodeClick / onDesignNodeClick
    │ 查找 pairs 中对应配对
    ▼
selectedPair.value 更新
    │ props 下发到 ImagePanel / DiffReport
    ▼
Canvas 高亮矩形 + 差异列表滚动定位
```

---

## 七、API 层说明

| 函数 | 方法 | 路径 | 说明 |
|---|---|---|---|
| `fetchCases(platform)` | GET | `/api/cases?platform=` | 获取内置案例列表 |
| `checkCase(id, platform)` | POST | `/api/check/case/:id` | 分析内置案例 |
| `checkUpload(...)` | POST | `/api/check/upload` | 上传文件分析 |
| `imageUrl(id, type, platform)` | — | `/api/cases/:id/image/:type` | 案例图片 URL |
| `reportTrack(params)` | POST | 待接入 | 入口埋点打点（预留） |

**请求拦截器**：`request.ts` 中 axios 实例每次请求前自动从 `localStorage.uiplusToken` 读取 token，写入 `Authorization: Bearer <token>` 请求头。

---

## 八、共享常量说明

| 文件 | 常量 | 用途 |
|---|---|---|
| `api/constants.ts` | `PlatformKey` / `PLATFORMS` / `DEFAULT_PLATFORM` | 平台类型定义与枚举，API 层内外共用 |
| `api/constants.ts` | `ZH_BY_KEY` / `KEY_BY_ZH` | 平台 key ↔ 中文名双向映射 |
| `views/utils/constants.ts` | `CASE_NAMES_BY_PLATFORM` | 各平台案例 ID → 中文展示名 |
| `views/utils/constants.ts` | `DEBUG_COLORS` | Debugger 模式节点着色调色板（12色） |
| `views/utils/constants.ts` | `FILE_SLOTS` | 文件拖拽识别规则（key / 匹配函数） |

---

## 九、画布缩放功能（Ctrl + 滚轮）

### 9.1 功能描述

鼠标悬停在任意一侧画布上，按住 `Ctrl` 并滚动鼠标滚轮，可对两侧画布**同步**放大/缩小。

| 操作 | 效果 |
|---|---|
| Ctrl + 向上滚动 | 以鼠标为中心放大（factor = 1.1） |
| Ctrl + 向下滚动 | 以鼠标为中心缩小（factor = 1/1.1）|
| 窗口 resize | 两侧自动重置回 1× |
| 最小/最大缩放 | 1× ~ 100× |

### 9.2 DOM 层级（`ImagePanel.vue`）

```
.img-panel
└── .img-wrapper
    └── .zoom-clip           ← 固定容器，位置稳定，用于坐标计算
        └── .zoom-layer      ← 绑定 :style="zoomStyle"，接受 CSS transform: scale()
            ├── <img>        ← 背景图片
            └── <canvas>     ← 事件监听层（click / dblclick / mousemove / wheel）
```

`.zoom-layer` 通过计算属性驱动变换：

```js
const zoomStyle = computed(() => ({
  transform: `scale(${zoomScale.value})`,
  transformOrigin: `${zoomOriginX.value}px ${zoomOriginY.value}px`,
}))
```

### 9.3 事件处理（`onCanvasWheel`）

```js
// onMounted 时注册，必须 passive: false 才能调用 preventDefault()
canvasRef.value?.addEventListener('wheel', onCanvasWheel, { passive: false })

function onCanvasWheel(e) {
  if (!e.ctrlKey) return          // 非 Ctrl 滚轮不处理
  e.preventDefault()              // 阻止浏览器页面缩放
  const factor = e.deltaY < 0 ? 1.1 : 1 / 1.1
  // 计算鼠标在 zoom-clip 内的归一化坐标（0-1）
  const rect = zoomClipRef.value.getBoundingClientRect()
  const normX = (e.clientX - rect.left) / rect.width
  const normY = (e.clientY - rect.top)  / rect.height
  applyZoom(factor, normX, normY)
  emit('zoom', { factor, normX, normY })   // 触发对侧联动
}
```

### 9.4 缩放应用（`applyZoom`）

```js
// normX/normY：鼠标相对 zoom-clip 的归一化坐标
// 将归一化坐标转为像素坐标作为 transform-origin，保证以鼠标为中心缩放
function applyZoom(factor, normX, normY) {
  zoomOriginX.value = normX * clip.clientWidth
  zoomOriginY.value = normY * clip.clientHeight
  zoomScale.value   = Math.max(1, Math.min(100, zoomScale.value * factor))
  nextTick(draw)
}
```

### 9.5 双侧联动（`ReportPage.vue`）

两个 `ImagePanel` 实例通过 `defineExpose({ applyZoom, resetZoom })` 暴露方法，`ReportPage` 交叉调用实现同步：

```
用户在开发侧画布 Ctrl+滚轮
    │ emit('zoom', { factor, normX, normY })
    ▼
onDevPanelZoom()                    [ReportPage]
    │ designPanelRef.value?.applyZoom(factor, normX, normY)
    ▼
设计侧画布同步缩放到相同比例和视角
```

```
用户在设计侧画布 Ctrl+滚轮
    │ emit('zoom', { factor, normX, normY })
    ▼
onDesignPanelZoom()                 [ReportPage]
    │ devPanelRef.value?.applyZoom(factor, normX, normY)
    ▼
开发侧画布同步缩放到相同比例和视角
```

```js
// ReportPage.vue 中的联动处理
function onDevPanelZoom({ factor, normX, normY }) {
  designPanelRef.value?.applyZoom(factor, normX, normY)
}
function onDesignPanelZoom({ factor, normX, normY }) {
  devPanelRef.value?.applyZoom(factor, normX, normY)
}
// 窗口 resize 时重置两侧缩放
function onWindowResize() {
  devPanelRef.value?.resetZoom()
  designPanelRef.value?.resetZoom()
}
```

### 9.6 关键实现细节

| 细节 | 说明 |
|---|---|
| `passive: false` | wheel 事件默认 passive，必须显式关闭才能 `preventDefault()` 阻止浏览器缩放 |
| 变换原点不平移 | 只用 `scale()`，通过 `transform-origin` 跟随鼠标，不做 `translate` |
| 使用 `zoom-clip` 而非 `canvas` | canvas 受 scale 影响，坐标不稳定；`zoom-clip` 无 transform，getBoundingClientRect 可靠 |
| Inspector 定位不受缩放影响 | `updateInspectorPos` 也基于 `zoom-clip` 的稳定坐标计算，缩放后 Inspector 仍贴近节点 |
| canvas 在 scale 层内重绘 | `draw()` 后 canvas 大小 = 图片 clientWidth/Height（已被 scale 拉伸），节点矩形按比例渲染正确 |

---

## 十、节点详情框（ImagePanel.vue）

节点点击后，`ImagePanel.vue` 的 `displayStyle` computed 从 `inspectorNode.style` 读取属性，逐项展示在 `.node-inspector` 浮层中。

### 展示项（按 文字限定 → 容器限定 → 公用 → 详情框独有 的顺序）

| # | 属性 | 标签 | 类型限定 | 显示条件 | 参与比对 |
|---|---|---|---|---|---|
| 1 | `fontSize` | 字号 | 文字限定 | `!= null` | ✅ |
| 2 | `fontWeight` | 字重 | 文字限定 | `!= null` | ✅ |
| 3 | `fontColor` | 颜色 | 文字限定 | truthy | ✅ |
| 4 | `fontFamily` | 字体 | 文字限定 | truthy | ✅（web 平台除外） |
| 5 | `backgroundColor` | 填充 | 容器限定 | truthy（同时显示色块） | ✅ |
| 6 | `borderRadius` | 圆角 | 容器限定 | truthy | ✅ |
| 7 | `border.width` | 描边宽度 | 容器限定 | `!= null` | ✅ |
| 8 | `border.color` | 描边颜色 | 容器限定 | truthy（同时显示色块） | ✅ |
| 9 | `opacity` | 不透明度 | 公用 | `!= null && !== 1` 或有 diff | ✅ |
| 10 | `shadow` | 阴影 | 公用 | truthy | ✅ |
| 11 | `blur` | 模糊 | 公用 | truthy | ✅ |
| 12 | `textAlign` | 对齐 | 文字限定 | truthy | ❌ 仅展示 |
| 13 | `lineHeight` | 行高 | 文字限定 | `!= null` | ❌ 仅展示 |
| 14 | `letterSpacing` | 字间距 | 文字限定 | `!= null && !== 0` | ❌ 仅展示 |
| 15 | `padding` | 内边距 | 容器限定 | truthy | ❌ 仅展示 |
| 16 | `itemSpacing` | 间距 | 容器限定 | `!= null` | ❌ 仅展示 |

> 解析阶段已按类型限定 style 属性，非限定的属性不会存在于 `inspectorNode.style` 中，因此容器节点详情框只显示 8-16 项，文本节点只显示 1-7 + 9 + 15-16 项。
>
> `textAlign`（对齐）、`lineHeight`（行高）、`letterSpacing`（字间距）、`padding`（内边距）、`itemSpacing`（间距）仅在详情框展示，不参与样式比对（`styleComparator.js` 中对应函数未接入 `compareStyles`）。`fontFamily` 在 web 平台不参与比对。

---

## 十一、URL 变更场景

所有 URL 修改均通过 `utils/urlParams.ts` 中的 `setUrlParams` / `removeUrlParams` 实现，统一使用 `window.history.replaceState`（替换当前历史记录，不增加回退栈）。**所有场景均自动保留 `debugger=1` 参数**（若当前 URL 中存在），无需调用方处理。

| 函数 | 作用 | 签名 |
|---|---|---|
| `setUrlParams` | 写入参数到 URL | `(params: Record<string, string>)` |
| `removeUrlParams` | 从 URL 删除指定 key | `(keys: string[])` |

| # | 场景 | 触发时机 | 操作 | 位置 |
|---|---|---|---|---|
| 1 | 传送码参数清除 | 外部跳转携带 `url` + `randomCode`，`initApp()` 第 7 步 | 取出后清除 `url` 和 `randomCode`，保留其余参数 | `init/handleTransferCode.ts:21` |
| 2 | 初始化失败 | `initApp()` 抛出异常且 `e.clearUrl === true` | 清空 URL 参数，保留 `debugger` | `ConsistencyView.vue:613` |
| 3 | 页面切换 | `onSelectPage()` 切换到已有页面后 | 写入 `deliverableId/pageId/versionId` | `ConsistencyView.vue:1046` |
| 4 | 重新对比存档 | `submitRerunVersion()` 存档后 | 写入 `deliverableId/pageId/versionId` | `ConsistencyView.vue:816` |
| 5 | 上传对比存档 | `submitResult()` 首次上传存档后 | 写入 `deliverableId/pageId/versionId` | `ConsistencyView.vue:1147` |
| 6 | 历史版本切换 | `onHistoryView()` 点击历史版本 | **replaceState + reload**（必须刷新页面重新加载） | `ConsistencyView.vue:1062-1063` |

### URL 参数说明

| 参数 | 含义 | 三参数齐全时的行为 |
|---|---|---|
| `deliverableId` | 交付件 ID | 初始化时走 `loadDeliverable()` 拉取历史存档数据 |
| `pageId` | 页面 ID | 同上 |
| `versionId` | 版本 ID | 同上，定位到具体版本 |
| `url` | 设计稿 URL | 仅外部跳转携带，`handleTransferCode()` 处理后立即清除 |
| `randomCode` | 传送码 | 仅外部跳转携带，取出回填输入框后立即清除 |
| `debugger` | 调试模式开关 | `debugMode.value = route.query.debugger === '1'`，**所有场景均保留不清除** |

> 场景 6（历史版本切换）是唯一使用 `window.location.reload()` 的场景，因为需要重新解析版本数据，纯 `replaceState` 无法触发组件重新初始化。
